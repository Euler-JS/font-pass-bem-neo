{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs", "npm"],
      "aptPkgs": []
    },
    "install": {
      "cmds": ["npm ci"]
    },
    "build": {
      "cmds": ["npm run build"]
    }
  },
  "start": {
    "cmd": "npx serve -s build -l 80"
  }
}
